<h2>New cachegroup type</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_cachegroup_type_path(@conn, :create) %>

<%= link "Back", to: html_cachegroup_type_path(@conn, :index) %>
