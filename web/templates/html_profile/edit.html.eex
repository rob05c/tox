<h2>Edit profile</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_profile_path(@conn, :update, @profile) %>

<%= link "Back", to: html_profile_path(@conn, :index) %>
