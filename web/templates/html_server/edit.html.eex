<h2>Edit Server</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_server_path(@conn, :update, @server) %>

<%= link "Back", to: html_server_path(@conn, :index) %>
