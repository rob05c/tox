<h2>New parameter</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_parameter_path(@conn, :create) %>

<%= link "Back", to: html_parameter_path(@conn, :index) %>
