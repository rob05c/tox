<h2>New cachegroup parameter</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_cachegroup_parameter_path(@conn, :create) %>

<%= link "Back", to: html_cachegroup_parameter_path(@conn, :index) %>
