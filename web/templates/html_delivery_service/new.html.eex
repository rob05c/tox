<h2>New html delivery service</h2>

<%= render "form.html", changeset: @changeset,
                        action: html_delivery_service_path(@conn, :create) %>

<%= link "Back", to: html_delivery_service_path(@conn, :index) %>
